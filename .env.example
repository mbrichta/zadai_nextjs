# reCAPTCHA (site key is client-safe; secret is server-only)
VITE_RECAPTCHA_SITE_KEY=
CAPTCHA_SECRET_KEY=

# Sentry (configured via instrument.server.mjs)
SENTRY_DSN=

# Better Auth (scaffold default — not used by marketing site yet)
BETTER_AUTH_SECRET=
BETTER_AUTH_URL=http://localhost:3000
